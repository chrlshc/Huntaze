# -----------------------------
# Huntaze Starter - Environment
# Copy this file to `.env.local` and fill values.
# -----------------------------

# --- AWS / Analytics (required) ---
# Primary region for DynamoDB and SDK calls
AWS_REGION=us-east-1
# or use the Next.js public var if you prefer
# NEXT_PUBLIC_AWS_REGION=us-east-1

# DynamoDB table for analytics events
ANALYTICS_TABLE=huntaze-analytics-events
# TTL retention (days). Deletion is asynchronous in DynamoDB
ANALYTICS_TTL_DAYS=60

# OAuth tokens persistence table (DynamoDB)
TOKENS_TABLE=huntaze-oauth-tokens

# Optional: restrict /r redirect destinations (comma-separated)
# R_ALLOWED_HOSTS=tiktok.com,www.tiktok.com,instagram.com,www.instagram.com,reddit.com,www.reddit.com,onlyfans.com,www.onlyfans.com,huntaze.com,www.huntaze.com
# Optional: HMAC signature for /r links (server-only secret)
# R_SIGNING_SECRET=change-me-strong-secret

# --- App URL (used to form OAuth redirect URIs if not provided) ---
# Public base URL of your app (https origin)
NEXT_PUBLIC_APP_URL=https://localhost:3000

# --- TikTok OAuth ---
TIKTOK_CLIENT_KEY=
TIKTOK_CLIENT_SECRET=
NEXT_PUBLIC_TIKTOK_REDIRECT_URI=https://app.huntaze.com/auth/tiktok/callback

# --- Reddit OAuth ---
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=
REDDIT_USER_AGENT=Huntaze:v1.0.0 (by u/yourusername)
NEXT_PUBLIC_REDDIT_REDIRECT_URI=https://app.huntaze.com/auth/reddit/callback

# --- Instagram Basic Display (fallback) ---
INSTAGRAM_CLIENT_ID=
INSTAGRAM_CLIENT_SECRET=
NEXT_PUBLIC_INSTAGRAM_REDIRECT_URI=https://app.huntaze.com/auth/instagram/callback

# --- Instagram Graph (Business/Creator) ---
# Enable IG Graph endpoints in starter
IG_GRAPH_ENABLED=true
# Graph API version (default v20.0)
IG_GRAPH_VERSION=v20.0
# Provide here or pass in the POST body to /api/instagram/graph/snapshot
IG_GRAPH_USER_ID=
IG_GRAPH_ACCESS_TOKEN=

# --- Scheduler (no OF autopost) ---
# SQS publishers for non-OF platforms (optional; leave empty to disable routing)
SQS_PUBLISHER_INSTAGRAM_URL=
SQS_PUBLISHER_TIKTOK_URL=
SQS_PUBLISHER_REDDIT_URL=

# --- Secure internal cron/invoke (optional) ---
# Protects /api/cron/refresh-oauth (Bearer <CRON_SECRET>)
CRON_SECRET=
