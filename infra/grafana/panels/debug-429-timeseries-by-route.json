{
  "type": "timeseries",
  "title": "Debug API 429 rate by route (5m)",
  "targets": [
    { "refId": "A", "expr": "sum by (route) (rate(social_debug_api_rate_limited_total[5m]))", "legendFormat": "{{route}}" }
  ],
  "options": {
    "legend": { "displayMode": "list", "placement": "bottom" },
    "tooltip": { "mode": "single" }
  },
  "gridPos": { "h": 8, "w": 24, "x": 0, "y": 0 }
}

