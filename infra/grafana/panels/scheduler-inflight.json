{
  "type": "timeseries",
  "title": "Insights Scheduler Inflight (items)",
  "targets": [
    { "refId": "A", "expr": "insights_scheduler_inflight", "legendFormat": "{{queue}}" }
  ],
  "options": {
    "legend": { "displayMode": "list", "placement": "bottom" },
    "tooltip": { "mode": "single" }
  },
  "gridPos": { "h": 6, "w": 24, "x": 0, "y": 8 }
}

