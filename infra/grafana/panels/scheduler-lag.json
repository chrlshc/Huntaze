{
  "type": "timeseries",
  "title": "Insights Scheduler Lag (seconds)",
  "targets": [
    { "refId": "A", "expr": "insights_scheduler_lag_seconds", "legendFormat": "{{queue}}" }
  ],
  "options": {
    "legend": { "displayMode": "list", "placement": "bottom" },
    "tooltip": { "mode": "single" }
  },
  "fieldConfig": {
    "defaults": {
      "thresholds": { "mode": "absolute", "steps": [
        { "color": "green", "value": null },
        { "color": "orange", "value": 180 },
        { "color": "red", "value": 300 }
      ] }
    }
  },
  "gridPos": { "h": 8, "w": 24, "x": 0, "y": 0 }
}

