{
  "type": "timeseries",
  "title": "Insights Error % by platform/kind",
  "targets": [
    {
      "refId": "A",
      "expr": "100 * sum by (platform, kind) (rate(social_insights_fetch_total{status=\\\"error\\\"}[$__rate_interval])) / sum by (platform, kind) (rate(social_insights_fetch_total[$__rate_interval]))",
      "legendFormat": "{{platform}} / {{kind}}"
    }
  ],
  "fieldConfig": {
    "defaults": { "unit": "percentunit" }
  },
  "options": {
    "legend": { "displayMode": "list", "placement": "bottom" },
    "tooltip": { "mode": "single" }
  },
  "gridPos": { "h": 8, "w": 24, "x": 0, "y": 16 }
}

