# AI System Production Environment Variables
# Copy these to your Amplify Console Environment Variables

# ============================================
# CRITICAL: Google Gemini AI Configuration
# ============================================
# Get your API key from: https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.0-flash-exp

# ============================================
# CRITICAL: AWS ElastiCache Redis
# ============================================
# For AI rate limiting - must be in same VPC as Amplify
ELASTICACHE_REDIS_HOST=huntaze-redis-production.asmyhp.0001.use1.cache.amazonaws.com
ELASTICACHE_REDIS_PORT=6379

# ============================================
# Database (should already be configured)
# ============================================
DATABASE_URL=postgresql://username:password@host:5432/database

# ============================================
# NextAuth (should already be configured)
# ============================================
NEXTAUTH_URL=https://your-production-domain.com
NEXTAUTH_SECRET=your-nextauth-secret-here

# ============================================
# AWS Configuration (should already be set)
# ============================================
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=REDACTED-access-key
AWS_SECRET_ACCESS_KEY=REDACTED-secret-key

# ============================================
# Optional: AI System Configuration
# ============================================
# Override default quotas (optional)
# QUOTA_STARTER_USD=10
# QUOTA_PRO_USD=50
# QUOTA_BUSINESS_USD=999999

# Override default rate limits (optional)
# RATE_LIMIT_STARTER=50
# RATE_LIMIT_PRO=100
# RATE_LIMIT_BUSINESS=500

# Enable/disable AI features (optional)
# AI_ENABLED=true

# ============================================
# Monitoring (optional but recommended)
# ============================================
# CloudWatch log group
# CLOUDWATCH_LOG_GROUP=/aws/amplify/huntaze/ai-system

# Alert thresholds
# AI_COST_ALERT_THRESHOLD=200
# AI_ERROR_RATE_THRESHOLD=0.05

# ============================================
# Notes
# ============================================
# 1. Never commit this file with real values
# 2. Set these in Amplify Console > Environment Variables
# 3. Restart deployment after changing variables
# 4. Test with verify-ai-deployment.sh script
