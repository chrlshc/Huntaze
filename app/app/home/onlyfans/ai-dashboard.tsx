'use client';

export default function OnlyFansHomeDashboard() {
  return (
    <div className="min-h-[60vh] flex flex-col items-center justify-center gap-4 p-10 text-center">
      <h1 className="text-3xl font-semibold">OnlyFans Home Dashboard</h1>
      <p className="text-muted-foreground max-w-2xl">
        Dashboard widgets are coming soon. This placeholder keeps the route functional so the
        build can succeed while the production UI is implemented.
      </p>
    </div>
  );
}
