plete"
omocal test c"✅ L""
echo 

echo ist"
fiex not does .env.local  echo "❌se
 
  fi
elng"issiis mURL  NEXTAUTH_o "❌se
    echset"
  elUTH_URL is  NEXTAo "✅ech
    henl; tv.locaenURL=" .UTH_q "NEXTA -f grep
  i  fi
  g"
T is missinUTH_SECRETA"❌ NEX
    echo   elseis set"
RET ECTH_SNEXTAUho "✅ en
    eclocal; thET=" .env.EXTAUTH_SECRrep -q "N  if gts"
  
.local exischo "✅ .env  e ]; then
alenv.loc [ -f .les..."
if variabntronmeking enviChecho "3️⃣ cho ""
ecfi

e
ATUS"o "❌ $STlse
  echS"
e✅ $STATU echo "]; then
  = "200" ATUS"$ST")
if [ h/signinpi/aut0/a300alhost:http://loc" de}%{http_coll -w " -o /dev/nu$(curl -s "
STATUS=in:uth/sign  /api/aecho -n " nin
est sig"
fi

# TTUSo "❌ $STAch
  eelseATUS"
ho "✅ $STn
  eche]; t" = "200" S"$STATUion)
if [ sessuth/00/api/a:30ocalhost" http://lde}ttp_cow "%{hv/null --o /derl -s (cu=$: "
STATUSessionauth/s"   /api/ -n on
echosessiTest i

# STATUS"
fho "❌ $ ec"
else
 "✅ $STATUS  echo en
200" ]; th = "S"TU "$STA
if [)/csrf/api/auth:3000//localhost" http:}ttp_code"%{hull -w ev/n /dl -s -oS=$(curf: "
STATUapi/auth/csr  / " f
echo -nsrTest c

# fiSTATUS"
"❌ $echo lse
  ATUS"
e$ST
  echo "✅ ]; then = "200" "$STATUS"rs)
if [ /provideapi/auth3000/host:ttp://localtp_code}" hl -w "%{htul /dev/nurl -s -o(c
STATUS=$rs: "deroviuth/ppi/ao -n "   /achproviders
e

# Test .."h endpoints. NextAut currenttingTesho "2️⃣ 
eccho ""1
fi

ev"
  exit pm run de nth:start it wiease  echo "Plrunning"
 er is NOT serv "❌ Dev e
  echoning"
elsun server is ro "✅ Deven
  ech2>&1; thv/null 0 > /dealhost:300http://locif curl -s "
 running... isrver if dev sehecking⃣ C
echo "1️nings runv server ick if de
# Che ""
ly"
echoocalal Version LtAuth Minimting Nexcho "🧪 Tesh

e#!/bin/bas