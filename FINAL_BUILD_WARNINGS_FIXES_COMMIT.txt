fix: Resolve all remaining Next.js build warnings for clean staging deployment

Comprehensive cleanup of build warnings for production-ready deployment:

1. React Window Dynamic Import:
   - components/ui/VirtualizedList.tsx: Fixed FixedSizeList import error
   - Used next/dynamic with SSR disabled to prevent build warnings
   - Resolves "Attempted import error: 'FixedSizeList' is not exported"

2. Heroicons v2 Compatibility Complete:
   - components/smart-onboarding/analytics/OnboardingAnalyticsDashboard.tsx
   - components/smart-onboarding/analytics/InterventionEffectivenessReporting.tsx
   - Updated TrendingUpIcon → ArrowTrendingUpIcon for v2 compatibility

3. Onboarding Component Imports Fixed:
   - components/onboarding/OnboardingWizard.tsx: Fixed default imports
   - ProgressTracker and StepNavigation now use proper default imports
   - Resolves component import/export mismatches

4. Smart Onboarding Configuration:
   - lib/smart-onboarding/services/behavioralAnalyticsService.ts
   - WEBSOCKET_CHANNELS correctly imported from ../config/redis
   - Proper service configuration and channel management

5. Database Alias Resolution:
   - src/lib/db.ts: Clean re-export via lib/db/index
   - All database utilities (query/getClient/getPool/db) properly exported
   - Resolves @/lib/db alias import issues

6. ROI Tracker Constructor Fix:
   - lib/smart-onboarding/services/interventionEffectivenessTracker.ts
   - Concrete InterventionEffectivenessTrackerImpl instance exported
   - Resolves TypeError constructor issues in API routes

Build Status:
✅ Local build successful with SKIP_ENV_VALIDATION=1 npx next build
✅ No compilation warnings remaining
✅ Only non-blocking ENOENT traced files message (Next 15 specific)
✅ All critical import/export issues resolved

Non-blocking warnings remaining:
- npm ci EBADENGINE (camera-controls Node >=24.4 requirement)
- Some deprecated packages (informational only)

This ensures clean Amplify staging deployment without build warnings.