version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20 && nvm use 20
        - echo "[PREBUILD] node=$(node -v) npm=$(npm -v)"
        - npm ci --no-audit --no-fund || npm i --no-audit --no-fund
        - echo "PREBUILD_DONE"
    build:
      commands:
        - echo "BUILD_START"
        - export BUILD_REDIS_MOCK=1
        # Write .env.production BEFORE any scripts that need it
        - |
          cat > .env.production <<EOF
          OPENAI_API_KEY=${OPENAI_API_KEY}
          OPENAI_MODEL=${OPENAI_MODEL}
          AZURE_OPENAI_ENDPOINT=${AZURE_OPENAI_ENDPOINT}
          AZURE_OPENAI_DEPLOYMENT=${AZURE_OPENAI_DEPLOYMENT}
          AZURE_OPENAI_API_KEY=${AZURE_OPENAI_API_KEY}
          AZURE_OPENAI_API_VERSION=${AZURE_OPENAI_API_VERSION}
          USE_AZURE_RESPONSES=${USE_AZURE_RESPONSES}
          ENABLE_AZURE_AI=${ENABLE_AZURE_AI}
          SQS_URL=${SQS_URL}
          DATABASE_URL=${DATABASE_URL}
          JWT_SECRET=${JWT_SECRET}
          STRIPE_SECRET_KEY=${STRIPE_SECRET_KEY}
          NEXT_PUBLIC_APP_URL=${NEXT_PUBLIC_APP_URL}
          NEXT_PUBLIC_API_URL=${NEXT_PUBLIC_API_URL}
          FROM_EMAIL=${FROM_EMAIL}
          AWS_REGION=${AWS_REGION}
          NEXTAUTH_URL=${NEXTAUTH_URL}
          AUTH_SECRET=${AUTH_SECRET}
          FACEBOOK_APP_ID=${FACEBOOK_APP_ID}
          FACEBOOK_APP_SECRET=${FACEBOOK_APP_SECRET}
          REDDIT_CLIENT_ID=${REDDIT_CLIENT_ID}
          REDDIT_CLIENT_SECRET=${REDDIT_CLIENT_SECRET}
          EOF
        - echo "[BUILD] wrote .env.production"
        # Initialize database tables if needed (now DATABASE_URL is available)
        - npm run db:init:safe || echo "DB already initialized"
        - npm run build
        - echo "BUILD_DONE"
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
