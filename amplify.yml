version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --include=dev --legacy-peer-deps || npm install --include=dev --legacy-peer-deps
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
